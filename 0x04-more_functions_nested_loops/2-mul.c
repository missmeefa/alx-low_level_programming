#include "main.h"
/**
 * mul - multiple two integers (0 through 9).
 * @x: x - Variable
 * @y: y - variable
 * Return: Always 0.
 */
int mul(int x, int y)
{
	int mul;

	mul = x * y;

	return (mul);
}
